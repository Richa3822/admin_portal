import { API_BASE_URL } from "../config";

export const WAITING_TIMING = 500
export const LIMIT = 10
export const PAGES_COUNT = 3
export const ORDER_URL = `${API_BASE_URL}order/admin`;
export const ORDER_BY_ID_URL = `${API_BASE_URL}order/admin`;
export const UPDATE_ORDER_URL = `${API_BASE_URL}order`;